// const socketReducer = (state =[], action) => {
//   switch (action.type) {
//     case 'NEW_ROLE':
//       return state.concat(action.data)
//     case 'INIT_ROLES':
//       return state.concat(action.data)
//     default:
//       return state
//   }
// }

// export const removeUser = () => {
//   type: 'REMOVE_USER_FROM_SOCKET',
//   socket: {
//     event: 'remove_user', {username:props.user.username, name: props.user.name, id: props.user.id})
//   }
// }

// export default roleReducer
